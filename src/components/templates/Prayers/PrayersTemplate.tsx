import { Text } from 'react-native';

import ViewBase from '../../shared/ViewBase/ViewBase';

export default function PrayersTemplate(): JSX.Element {
  return (
    <ViewBase>
      <Text>Tela de orações</Text>
    </ViewBase>
  );
}
